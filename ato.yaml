ato-version: ">=0.12.0"

# Project configuration for Pico 2W Quadcopter Flight Controller
build:
  # Main flight controller design
  default:
    entry: hardware/main.ato

  # Test pickable components (resistors)
  test_pickable:
    entry: test_pickable.ato

# Dependencies
dependencies: []

# Metadata
meta:
  name: "pico-quadcopter-flight-controller"
  description: "Raspberry Pi Pico 2W based quadcopter flight controller PCB"
  author: "pete5"
  license: "MIT"
  version: "0.1.0"